describe('express-test', function() {
  it('health checker can be instructed to embed itself in express')
  it('a custom path prefix can be used')
  it('custom middleware can be provided')
  it('it is possible to only run internal checks')
})