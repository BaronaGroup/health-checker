describe('office hours threshold test', function() {
  describe('threshold is ignored', function() {
    it('during weekends')
    it('before 9 am')
    it('after 5 pm')
  })

  it('details are provided even if there is no error')

  it('threshold is configurable')

  describe('within window', function () {
    it('recent enough events result in success')
    it('no recent enough events results in failure')
  })
})